version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Building static site"
        - ls -la
        - ls -la public/
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths: []